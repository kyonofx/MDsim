from setuptools import find_packages, setup

setup(
    name="mdsim",
    version="0.0.1",
    description="Machine learning models for MD simulation",
    packages=find_packages(),
    include_package_data=True,
)
